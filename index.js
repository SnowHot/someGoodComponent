const { isSortArray } = require("./fc/isSortArray");
const { flat } = require("./fc/flat");
const { is } = require("./fc/isArray");
const tap = require("./fc/tap");

module.exports = { isSortArray, flat, is, tap };
